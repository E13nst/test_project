# UI Automation Framework

Фреймворк для автоматизации UI-тестирования веб-сайтов на основе Python, Pytest, Selene и Allure.

## Особенности

✅ **Page Object паттерн** — чистая архитектура с разделением страниц и компонентов  
✅ **Автоожидания как в Selenide** — встроенные ожидания через Selene  
✅ **Allure-отчёты со скриншотами** — автоматическое прикрепление артефактов при падении  
✅ **Multi-site поддержка** — тесты для разных сайтов в отдельных директориях  
✅ **Строгая группировка** — тесты маппятся на пункты чек-листа через Allure иерархию

## Структура проекта

```
test_project/
├── CHECKLIST.md              # Чек-лист для ручного тестирования
├── Makefile                  # Команды для запуска тестов и управления проектом
├── requirements.txt          # Зависимости Python
├── pytest.ini               # Конфигурация pytest
└── tests/
    ├── conftest.py          # Главный conftest (импорт shared)
    ├── shared/              # Общие фикстуры и хуки для всех сайтов
    │   └── conftest.py      # Browser setup, Allure hooks
    └── sites/               # Директория для тестов разных сайтов
        └── elvirra_ru/      # Тесты для elvirra.ru
            ├── conftest.py  # Конфигурация для elvirra.ru
            ├── data/        # URL и тестовые данные
            ├── pages/       # Page Object классы
            ├── components/  # Переиспользуемые компоненты
            └── tests/       # Тестовые сценарии
```

## Установка

1. Клонируйте репозиторий
2. Установите зависимости (автоматически создаст виртуальное окружение):

**Через Makefile (рекомендуется):**
```bash
make install
```

**Или вручную:**
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

3. Убедитесь, что установлен Chrome/Chromium браузер

> 💡 **Примечание**: Makefile автоматически создаёт и использует виртуальное окружение `venv/`. Все команды тестов используют это окружение автоматически.

## Запуск тестов

### Через Makefile (рекомендуется)

Проект включает Makefile с удобными командами:

```bash
# Показать все доступные команды
make help

# Установить зависимости
make install

# Запустить все тесты
make test

# Запустить smoke-тесты
make test-smoke

# Запустить тесты по разделам
make test-general      # Чек-лист: Общее
make test-usability    # Чек-лист: Удобство сайта
make test-layout       # Чек-лист: Вёрстка

# Запустить тесты с Allure-отчётом
make test-allure       # Генерировать отчёт
make allure-serve      # Открыть отчёт в браузере
make allure-open       # Запустить тесты и открыть отчёт

# Очистка
make clean             # Очистить кэш
make clean-allure      # Удалить Allure-файлы
make clean-all         # Полная очистка
```

### Прямой запуск через pytest

#### Запустить все тесты

```bash
pytest
```

### Запустить тесты для конкретного сайта

```bash
pytest tests/sites/elvirra_ru/
```

### Запустить только smoke-тесты

```bash
pytest -m smoke
```

### Запустить тесты по группам чек-листа

```bash
# Только тесты "Общее"
pytest -m general

# Только тесты "Удобство сайта"
pytest -m usability

# Только тесты "Вёрстка"
pytest -m layout
```

### Запустить с генерацией Allure-отчёта

```bash
# Запуск тестов с сохранением результатов
pytest --alluredir=allure-results

# Генерация и открытие отчёта
allure serve allure-results
```

## Иерархия Allure-отчётов

Каждый тест следует строгой иерархии для группировки в Allure:

```python
@allure.parent_suite("elvirra.ru")           # Название сайта
@allure.suite("Чек-лист: Общее")             # Раздел чек-листа
@allure.sub_suite("Сайт корректно открывается и доступен.")  # Конкретный пункт
```

### Правила именования:

- **parent_suite** — доменное имя сайта (например, `elvirra.ru`)
- **suite** — точное название раздела из CHECKLIST.md:
  - `Чек-лист: Удобство сайта`
  - `Чек-лист: Общее`
  - `Чек-лист: Вёрстка`
- **sub_suite** — точный текст пункта из чек-листа

Это обеспечивает полное соответствие Allure-отчёта структуре чек-листа.

## Добавление нового сайта

Для добавления тестов нового сайта:

1. Создайте директорию по шаблону `tests/sites/<domain_slug>/`
   - Например: `tests/sites/example_com/`
   
2. Скопируйте структуру из `elvirra_ru`:
   ```
   tests/sites/example_com/
   ├── __init__.py
   ├── conftest.py
   ├── data/
   │   └── urls.py
   ├── pages/
   │   ├── base_page.py
   │   └── home_page.py
   ├── components/
   └── tests/
       └── test_general.py
   ```

3. Обновите `data/urls.py` с BASE_URL нового сайта

4. В тестах используйте `@allure.parent_suite("example.com")`

5. Следуйте той же структуре suite/sub_suite из чек-листа

## Page Object паттерн

### Базовый класс (BasePage)

Все Page Object наследуются от `BasePage`, который предоставляет:
- Методы навигации (`open`, `click`)
- Методы проверок (`should_have_title_containing`, `should_be_visible`)
- Утилиты (`get_text`, `type_text`)

### Пример Page Object

```python
from tests.sites.elvirra_ru.pages.base_page import BasePage

class HomePage(BasePage):
    HEADER = "header"
    LOGO = ".logo"
    
    def open_home(self):
        self.open("/")
        return self
    
    def should_have_header(self):
        self.should_be_visible(self.HEADER)
        return self
```

### Компоненты

Переиспользуемые UI-блоки (header, footer, формы) выносятся в `components/`:

```python
class HeaderComponent:
    HEADER = "header"
    
    def should_be_visible(self):
        browser.element(self.HEADER).should(be.visible)
        return self
```

## Автоожидания (Selene)

Все действия и проверки используют встроенные ожидания Selene:

```python
browser.element(selector).should(be.visible)  # Ждёт видимости
browser.element(selector).should(be.clickable).click()  # Ждёт кликабельности
browser.element(selector).type(text)  # Ждёт доступности для ввода
```

Таймаут по умолчанию: **10 секунд** (настраивается в `tests/shared/conftest.py`)

## Allure-артефакты

При падении теста автоматически прикрепляются:
- 📸 Скриншот страницы
- 📄 HTML-код страницы
- 🔗 URL страницы

Это настроено в `tests/shared/conftest.py` через хук `pytest_runtest_makereport`.

## Маркеры pytest

Доступные маркеры (определены в `pytest.ini`):
- `@pytest.mark.smoke` — smoke-тесты
- `@pytest.mark.general` — тесты раздела "Общее"
- `@pytest.mark.usability` — тесты раздела "Удобство сайта"
- `@pytest.mark.layout` — тесты раздела "Вёрстка"

## Troubleshooting

### Браузер не запускается

Убедитесь, что установлен ChromeDriver:
```bash
pip install webdriver-manager
```

### Тесты падают с timeout

Увеличьте таймаут в `tests/shared/conftest.py`:
```python
browser.config.timeout = 15  # было 10
```

### Allure-отчёт не генерируется

Установите Allure командной строки:
```bash
# macOS
brew install allure

# Linux
sudo apt-add-repository ppa:qameta/allure
sudo apt-get update
sudo apt-get install allure
```

## Контакты и поддержка

Для вопросов и предложений создавайте Issue в репозитории.

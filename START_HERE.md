# 🚀 Начните здесь!

Добро пожаловать в UI Automation Framework для тестирования веб-сайтов.

## ⚡ Быстрый старт (3 шага)

### 1️⃣ Установите зависимости
```bash
make install
```

### 2️⃣ Запустите тесты
```bash
make test-smoke
```

### 3️⃣ Посмотрите Allure-отчёт
```bash
make allure-open
```

> 💡 **Совет**: Выполните `make help` чтобы увидеть все доступные команды!

## 📚 Документация

Выберите документ в зависимости от вашей задачи:

### Для быстрого старта
- **[QUICKSTART.md](QUICKSTART.md)** — команды запуска и базовые примеры
- **[MAKEFILE.md](MAKEFILE.md)** — полный справочник по командам Makefile

### Для изучения архитектуры
- **[README.md](README.md)** — полная документация фреймворка
- **[EXAMPLE_TEST.md](EXAMPLE_TEST.md)** — примеры кода с комментариями

### Для понимания реализации
- **[IMPLEMENTATION_SUMMARY.md](IMPLEMENTATION_SUMMARY.md)** — что реализовано
- **[PROJECT_METRICS.md](PROJECT_METRICS.md)** — метрики и статистика

### Исходный чек-лист
- **[CHECKLIST.md](CHECKLIST.md)** — чек-лист для ручного тестирования

## 🎯 Что уже работает

✅ **26 автоматизированных тестов** для elvirra.ru  
✅ **Page Object паттерн** с чистой архитектурой  
✅ **Автоожидания** как в Selenide (через Selene)  
✅ **Allure-отчёты** со скриншотами при падении  
✅ **Multi-site поддержка** — легко добавить новый сайт  

## 📊 Покрытие чек-листа

- **Чек-лист: Общее** → 7 тестов
- **Чек-лист: Удобство сайта** → 9 тестов
- **Чек-лист: Вёрстка** → 10 тестов

## 🏗️ Структура проекта

```
test_project/
├── README.md              ← Полная документация
├── QUICKSTART.md          ← Быстрый старт
├── Makefile               ← Команды для запуска тестов
├── requirements.txt       ← Зависимости
├── pytest.ini            ← Конфигурация
└── tests/
    ├── shared/           ← Общие фикстуры
    └── sites/
        └── elvirra_ru/   ← Тесты для elvirra.ru
            ├── pages/    ← Page Objects
            ├── components/ ← Компоненты
            └── tests/    ← Тестовые сценарии
```

## 🎓 Примеры команд

### Через Makefile (рекомендуется)

```bash
# Показать все команды
make help

# Все тесты
make test

# Только smoke
make test-smoke

# Только "Общее"
make test-general

# Только "Удобство"
make test-usability

# Только "Вёрстка"
make test-layout

# Тесты для elvirra.ru
make test-elvirra

# С Allure (запуск + открытие)
make allure-open

# Очистка
make clean-all
```

### Прямой запуск через pytest

```bash
# Все тесты
pytest

# Только smoke
pytest -m smoke

# Конкретный файл
pytest tests/sites/elvirra_ru/tests/test_general.py

# С подробным выводом
pytest -v

# С Allure
pytest --alluredir=allure-results
allure serve allure-results
```

## 🎨 Что увидите в Allure

Тесты сгруппированы по иерархии:

```
elvirra.ru (сайт)
├── Чек-лист: Общее
│   ├── Сайт корректно открывается и доступен. ✅
│   ├── Повторное открытие сайта выполняется без сбоев. ✅
│   └── ...
├── Чек-лист: Удобство сайта
│   ├── На каждой странице присутствует заголовок. ✅
│   ├── У всех полей есть подсказки... ✅
│   └── ...
└── Чек-лист: Вёрстка
    ├── Сайт имеет favicon. ✅
    ├── В консоли браузера отсутствуют ошибки JavaScript. ✅
    └── ...
```

При падении теста автоматически прикрепляются:
- 📸 Скриншот
- 📄 HTML страницы
- 🔗 URL

## 🔧 Добавление нового сайта

1. Создайте директорию:
```bash
mkdir -p tests/sites/example_com/{pages,components,tests,data}
```

2. Скопируйте структуру:
```bash
cp -r tests/sites/elvirra_ru/* tests/sites/example_com/
```

3. Обновите `data/urls.py` с новым BASE_URL

4. В тестах замените `@allure.parent_suite("elvirra.ru")` на `@allure.parent_suite("example.com")`

## 💡 Ключевые особенности

### Page Object
```python
class HomePage(BasePage):
    @allure.step("Открыть главную страницу")
    def open_home(self):
        self.open("/")
        return self  # Chaining
```

### Автоожидания
```python
# Вместо time.sleep()
browser.element(selector).should(be.visible)
browser.element(selector).should(be.clickable).click()
```

### Allure-иерархия
```python
@allure.parent_suite("elvirra.ru")
@allure.suite("Чек-лист: Общее")
@allure.sub_suite("Сайт корректно открывается и доступен.")
```

## 🆘 Проблемы?

### ChromeDriver не найден
```bash
pip install --upgrade webdriver-manager
```

### Allure не установлен
```bash
# macOS
brew install allure

# Linux
sudo apt-add-repository ppa:qameta/allure
sudo apt-get update
sudo apt-get install allure
```

### Тесты падают по timeout
Увеличьте таймаут в `tests/shared/conftest.py`:
```python
browser.config.timeout = 15  # было 10
```

## 📖 Дальнейшее чтение

1. Начните с **[QUICKSTART.md](QUICKSTART.md)** для первого запуска
2. Изучите **[EXAMPLE_TEST.md](EXAMPLE_TEST.md)** для понимания структуры
3. Прочитайте **[README.md](README.md)** для полной картины
4. Посмотрите **[IMPLEMENTATION_SUMMARY.md](IMPLEMENTATION_SUMMARY.md)** для деталей

## ✨ Готово к использованию!

Фреймворк полностью настроен и готов к:
- ✅ Запуску тестов
- ✅ Генерации отчётов
- ✅ Добавлению новых тестов
- ✅ Добавлению новых сайтов
- ✅ Демонстрации на собеседовании

**Удачи в тестировании! 🚀**

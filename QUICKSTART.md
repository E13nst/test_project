# Quick Start Guide

Быстрый старт для запуска UI-автотестов.

## 🚀 Быстрый старт через Makefile (рекомендуется)

```bash
# 1. Установить зависимости
make install

# 2. Запустить smoke-тесты
make test-smoke

# 3. Запустить тесты с Allure-отчётом
make allure-open
```

Показать все команды: `make help`

---

## 📋 Пошаговая инструкция

### 1. Установка зависимостей

**Через Makefile (рекомендуется):**
```bash
make install  # Автоматически создаст venv и установит зависимости
```

**Или вручную:**
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

> 💡 **Важно**: Makefile автоматически создаёт и использует виртуальное окружение. Все команды тестов работают через `venv/` автоматически.

### 2. Запуск smoke-тестов

**Через Makefile:**
```bash
make test-smoke
```

**Или вручную:**
```bash
pytest -m smoke -v
```

### 3. Генерация Allure-отчёта

**Через Makefile (рекомендуется):**
```bash
make allure-open  # Запустит тесты и откроет отчёт
```

**Или пошагово:**
```bash
# Запустить тесты с сохранением результатов
make test-allure

# Открыть отчёт в браузере
make allure-serve
```

## 4. Структура отчёта

В Allure вы увидите:
- **Parent Suite**: `elvirra.ru` (название сайта)
- **Suite**: `Чек-лист: Общее` / `Чек-лист: Удобство сайта` / `Чек-лист: Вёрстка`
- **Sub Suite**: конкретные пункты из CHECKLIST.md

## 5. Запуск отдельных групп

```bash
# Только "Общее"
pytest -m general

# Только "Удобство сайта"
pytest -m usability

# Только "Вёрстка"
pytest -m layout
```

## 6. Что проверяется

### Чек-лист: Общее (7 тестов)
- Доступность сайта
- Повторное открытие
- Кликабельность кнопок
- Наличие ссылок
- Работа основных элементов
- Навигационное меню
- Ссылка на главную страницу

### Чек-лист: Удобство сайта (9 тестов)
- Наличие заголовка страницы
- Единообразие элементов
- Стиль кнопок
- Ссылка на домашнюю страницу
- Доступность с клавиатуры
- Подсказки у полей
- Отсутствие наслоения элементов
- Пространство между элементами

### Чек-лист: Вёрстка (10 тестов)
- Наличие favicon
- Отсутствие JS ошибок
- Кодировка UTF-8
- Загрузка шрифтов
- Responsive: Desktop/Tablet/Mobile
- Функциональность кнопок
- Адаптивность форм

## 7. При падении теста

Автоматически прикрепляются:
- 📸 Скриншот
- 📄 HTML страницы
- 🔗 URL

## 8. Добавление тестов для нового сайта

```bash
# Создать структуру
mkdir -p tests/sites/example_com/{pages,components,tests,data}

# Скопировать шаблон
cp -r tests/sites/elvirra_ru/* tests/sites/example_com/

# Обновить BASE_URL в data/urls.py
# Обновить @allure.parent_suite("example.com") в тестах
```

## Troubleshooting

**Ошибка: ChromeDriver not found**
```bash
pip install --upgrade webdriver-manager
```

**Ошибка: allure command not found**
```bash
# macOS
brew install allure

# Linux
sudo apt-add-repository ppa:qameta/allure
sudo apt-get update
sudo apt-get install allure
```

**Тесты падают по timeout**
- Увеличьте `browser.config.timeout` в `tests/shared/conftest.py`
- Проверьте скорость интернет-соединения
